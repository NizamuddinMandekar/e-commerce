// Client-side password gate for the admin panel demo.
// This is NOT real security the password ships in the bundle and anyone
// with browser devtools can read it. Do not use this pattern for an admin
// panel that needs to protect real customer or business data; that requires
// a server-side auth check.
export const ADMIN_PASSWORD = 'ziudon';
